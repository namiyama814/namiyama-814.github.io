# DESIGN.md

## 目的と適用範囲

このドキュメントは、namiyama Portfolioの視覚表現、コンポーネント、レスポンシブ設計、アクセシビリティ、実装時の判断基準を定義します。個別ページの見た目だけでなく、将来の機能追加でも一貫した体験を維持するための設計基準です。

デザインシステムのドキュメントは、色や余白の値だけでなく、コンポーネントの目的、使用条件、アクセシビリティ、レスポンシブ挙動を共有する参照点として機能します。Primerは、ドキュメントを簡潔かつ本番品質に保ち、例示コードにもアクセシビリティ基準を反映することを推奨しています。[1] 本書もその考え方に従い、実装に直接使える方針に限定します。

> **基本方針:** 情報を少なく、明確に、力強く見せる。装飾を増やすのではなく、太いタイポグラフィ、余白、罫線、明暗反転で階層を作る。

## デザイン原則

| 原則 | 意図 | 実装上の判断 |
|---|---|---|
| 明快さ | コンテンツの優先順位を一目で理解できるようにする。 | 見出しは大きく、補助情報は小さく高い字間で置く。 |
| 構造の可視化 | セクションと情報群の境界を論理的に示す。 | 罫線はセクションの終端側に1本だけ置き、隣接する二重線を作らない。 |
| 高コントラスト | 白と黒に近い中立色だけで、読める状態と操作状態を明確にする。 | 明るい面は`--paper`、暗い面は`--ink`を使う。任意のグレーや装飾色を増やさない。 |
| 実用性 | 小さい画面、キーボード操作、文字選択でも使えるようにする。 | 線形レイアウトへの切替、十分な操作領域、フォーカス表示、背景別の選択色を守る。 |
| 言語の対称性 | 日本語と英語で同じ情報構造を提供する。 | `/`と`/en`、`/works`と`/en/works`、`/contact`と`/en/contact`を対応させる。 |

## デザイントークン

Primerは、色を生の値ではなく意味を持つトークンとして扱うことで、保守性とテーマ対応を高めています。[2] 本サイトは小規模な構成のため、まずはグローバルCSS変数で役割を固定します。

| トークン | 値 | 役割 |
|---|---:|---|
| `--paper` | `#FAFAFA` | 明るい面、暗い面の前景、選択時の反転色。 |
| `--ink` | `#252525` | 基本文字、明るい面の罫線、暗い面の背景。 |
| `--rule` | `#252525` | 明るい面における標準の境界線。 |
| `--rule-on-dark` | `#FAFAFA` | 暗い面における標準の境界線。 |

これらのトークン以外の色が必要になった場合は、まず「背景」「前景」「罫線」「状態」という役割を定義してから追加します。生の色値をコンポーネントへ直接追加して用途を曖昧にしないでください。

## タイポグラフィと間隔

本文はNoto Sans JPをベースにします。見出しは強い太さと負の字間、ラベルは小さなサイズと広い字間を使い、同じ書体の中で階層を作ります。

| 要素 | 目安 | 用途 |
|---|---|---|
| ヒーロー見出し | `clamp(2.65rem, 7vw, 6.6rem)`、`font-black` | ページの主題。2行以内を基本とする。 |
| セクション見出し | `clamp(2rem, 4.2vw, 4.5rem)`、`font-black` | セクションの目的を示す。 |
| 本文 | `text-sm`から`text-base`、`leading-8` | 読みやすい説明。1行が長くなりすぎない幅を設定する。 |
| ラベル | `10px`から`11px`、太字、広い字間 | 番号、分類、補助情報。 |

水平余白は小画面で`px-5`、中画面で`sm:px-8`、大画面で`lg:px-10`を基本にします。画面幅を問わず端に詰まりすぎないことを優先し、内容領域の最大幅は`max-w-7xl`に制限します。

## レイアウトと罫線

### グリッド

ホームページは大画面で12カラムを使い、セクションラベルを3カラム、内容を9カラムに分けます。ヒーローは8対4の構成です。中画面では内容を縦に積み、小画面では読み順と操作順が自然な1列にします。

| 画面幅 | レイアウト方針 | ナビゲーション |
|---|---|---|
| 375px前後 | 1列。ヒーローはテキストの後に暗いビジュアル面を置く。 | ブランド名とEN / JP切替を優先し、横並びナビゲーションは隠す。 |
| 768px前後 | 主に1列を維持し、カードは内容に応じて2列へ切り替える。 | 主要ナビゲーションを表示する。 |
| 1024px以上 | 12カラムの情報構造を有効にする。 | 主要ナビゲーション、言語切替、外部リンクを表示する。 |

### 罫線ルール

罫線が視覚的に混み合う問題を避けるため、`section-frame`を使うセクションは**終端側にだけ**罫線を配置します。次のセクションの先頭に同じ罫線を重ねないでください。カード群の内部境界には`gap-px`と親の背景色を使い、カード自身に重複した外枠を足さないことを基本とします。

暗いセクションでは白い罫線、明るいセクションでは黒い罫線を使います。色の反転は境界線にも一貫して適用します。

## コンポーネント仕様

| コンポーネント | 目的 | 必須要件 |
|---|---|---|
| `SiteHeader` | ブランド、主要導線、言語切替を提供する。 | 同じページ種別のEN / JP版へ移動できること。狭い画面で優先度の低いナビゲーションを隠すこと。 |
| `SiteFooter` | サイトの締めと補助導線を提供する。 | 年は`CurrentYear`でクライアント実行時に取得すること。CONTACTとGitHubへの導線を置くこと。 |
| `HomePage` | プロフィール、スキル、経歴、実績、SNSを紹介する。 | アンカーリンク、明暗セクション、カードの境界ルールを守ること。 |
| `WorksPage` | 全実績を一覧で見せる。 | 日本語・英語の説明を対応させ、公開先とコードの有無に応じてリンクを出すこと。 |
| `ContactPage` | 外部フォームサービスへの問い合わせ送信を提供する。 | 名前、メール、本文を必須にし、キーボードフォーカスが見えること。 |
| `CurrentYear` | フッターの西暦を表示する。 | `new Date().getFullYear()`をクライアント側で評価すること。 |

## 言語切替

言語切替はクライアント状態に依存せず、検索・共有・静的公開に適した対応ルートへ遷移します。

| ページ種別 | 日本語 | 英語 |
|---|---|---|
| ホーム | `/` | `/en` |
| 実績 | `/works` | `/en/works` |
| お問い合わせ | `/contact` | `/en/contact` |

新しいページを追加する場合は、可能な限り日本語・英語の対応ルートと、ヘッダーの言語切替先を同時に追加します。固有名詞、技術名、外部サービス名は不必要に翻訳しません。

## アクセシビリティ

アクセシビリティは追加機能ではなく、レイアウト、色、操作、文章すべての要件です。USWDSは、知覚可能、操作可能、理解可能、堅牢という4原則を基礎にし、コンポーネントを使うだけで満足せずページ単位の検証を求めています。[3]

### 選択表示

文字選択時の色は背景に応じて反転します。

| 背景 | 適用クラス | 選択背景 | 選択文字 |
|---|---|---|---|
| 明るい背景 | 既定 | `--ink` | `--paper` |
| 暗い背景 | `.selection-on-dark` | `--paper` | `--ink` |

`::selection`と`::-moz-selection`を両方定義します。暗い背景の要素を追加したときは、親要素に`.selection-on-dark`を付与してください。

### フォーカスと入力

フォーム要素と送信ボタンには、`focus-visible`で背景と反対色のリングを表示します。マウス操作だけでなく、キーボードとタッチ操作で完結できることを確認してください。USWDSも、十分な操作領域、線形レイアウト、ホバーのみに依存しない情報設計を推奨しています。[3]

### コンテンツ

画像には意味のある`alt`を付け、装飾目的のSVGには`aria-hidden="true"`を付けます。見出しレベルを飛ばさず、リンク文言だけで移動先を理解できるようにします。色だけで状態や意味を伝えないでください。

## ブラウザと実機相当の検証

実際の端末所有者による最終確認に加え、変更時には少なくともChromium系ブラウザのレスポンシブモードまたは同等のヘッドレス実行で確認します。

| 観点 | 375px前後 | 768px前後 | 1440px前後 |
|---|---|---|---|
| 横方向のはみ出し | 必須 | 必須 | 必須 |
| ヘッダーの情報量 | ブランドと言語切替が残る | 主要ナビゲーションが読める | すべての導線が読める |
| セクション罫線 | 二重線がない | グリッド切替で崩れない | 12カラムの線が揃う |
| 選択表示 | 明暗面で反転する | 明暗面で反転する | 明暗面で反転する |
| フォーム | 1列、入力しやすい | 2列入力が読める | 幅と余白が適切 |

擬似要素の選択表示は、ブラウザで計算済みスタイルを確認し、明るい面が`rgb(37, 37, 37)` / `rgb(250, 250, 250)`、暗い面がその逆になることを確認します。Safari、Firefox、実端末ではレンダリング差があり得るため、公開前にiOS Safari、Android Chrome、Firefoxでの目視確認を推奨します。自動検証は実端末テストの代替ではありません。

## 更新手順

1. 追加・変更する要素の役割を決め、既存トークンとコンポーネントで表現できるか確認します。
2. 明暗背景、罫線、レスポンシブ切替、EN / JPの対応関係を実装します。
3. `pnpm build`を実行して静的出力と型チェックを確認します。
4. 375px、768px、1440px前後で表示を確認し、明暗両方で文字選択・フォーカスを確認します。
5. 新しいコンポーネントに固有の注意点があれば、本書のコンポーネント仕様へ追記します。

## 参考資料

[1] [Primer — Documentation](https://primer.style/product/contribute/documentation/)

[2] [Primer — Color usage](https://primer.style/product/getting-started/foundations/color-usage/)

[3] [U.S. Web Design System — Accessibility](https://designsystem.digital.gov/documentation/accessibility/)
